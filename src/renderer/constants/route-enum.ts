export enum RouteEnum {
  settings = "/settings",
  imageConversion = "/imageConversion",
  biomeEditor = "/biomeEditor",
  levelEditor = "/levelEditor",
  stampEditor = "/stampEditor",
  terrainTextures = "/terrainTextures",
  assets = "/assets",
  database = "/database"
}
