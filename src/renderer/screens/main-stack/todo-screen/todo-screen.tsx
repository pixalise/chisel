import { FC } from "react";
import Section from "@/components/layout/section";

const TodoScreen: FC = () => {
  return (
    <Section title="Todos" copy="Simple project checklist for easier tracking and project management.">
      <div></div>
    </Section>
  );
};

export default TodoScreen;
